apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "demo-mex.fullname" . }}-test-connection"
  labels:
    {{- include "demo-mex.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "demo-mex.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
