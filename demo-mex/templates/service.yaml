apiVersion: v1
kind: Service
metadata:
  name: {{ include "demo-mex.fullname" . }}
  labels:
    {{- include "demo-mex.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "demo-mex.selectorLabels" . | nindent 4 }}
